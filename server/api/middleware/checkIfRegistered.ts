const usersJson = require('../../data/users.json');
const adminsJson = require('../../data/admins.json');

export const checkIfRegistered = (newUser) => {
  try {
  } catch (error) {
    console.log(error.message);
  }
};
